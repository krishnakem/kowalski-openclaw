/**
 * Single source of truth for the headless browser viewport dimensions.
 * The Electron window content area matches these dimensions exactly.
 */
export const KOWALSKI_VIEWPORT = { width: 1280, height: 900 } as const;
