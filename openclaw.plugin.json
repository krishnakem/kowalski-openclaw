{
    "id": "kowalski-openclaw",
    "name": "Kowalski",
    "description": "Browses your Instagram stories and feed and returns a markdown digest.",
    "version": "0.1.0",
    "kind": "capability",
    "main": "./src/plugin/index.ts",
    "contracts": {
        "tools": [
            "start_session",
            "login",
            "submit_verification_code",
            "run_digest",
            "get_session_status",
            "print_digest",
            "reset_memory",
            "reset_all",
            "stop_run",
            "end_session"
        ]
    },
    "skills": ["./skills"],
    "configSchema": {
        "type": "object",
        "properties": {
            "browserProfileDir": {
                "type": "string",
                "description": "Path to the persistent Playwright user-data directory. Used by both the login and run flows, which are fully headless. Leading ~ is expanded to the user's home directory at plugin-register time.",
                "default": "~/.kowalski/browser"
            },
            "scratchDir": {
                "type": "string",
                "description": "Ephemeral per-run working directory (raw frames, extraction sidecars, session memory). Safe to wipe between runs. Leading ~ is expanded at plugin-register time.",
                "default": "~/.kowalski/scratch"
            },
            "outputDir": {
                "type": "string",
                "description": "Durable output directory where finished analysis records land under analysis_records/<id>.json. Leading ~ is expanded at plugin-register time.",
                "default": "~/.kowalski/output"
            },
            "userName": {
                "type": "string",
                "description": "User's name — surfaced to the agents as context when they personalize the digest."
            },
            "location": {
                "type": "string",
                "description": "User's location (city / region) — surfaced to the agents as context."
            }
        },
        "additionalProperties": false
    }
}
